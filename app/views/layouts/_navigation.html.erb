 <a class="item">
          Chatroom
        </a>
        <a class="item">
          Messages
        </a>
        <div class="right menu">
          <div class="ui dropdown item">
            Language <i class="dropdown icon"></i>
            <div class="menu">
              <a class="item">Log out</a>
              <a class="item">Log in</a>
              <a class="item">Sign up</a>
            </div>
          </div>
          <div class="item">
          <% if logged_in? %>
              <%= link_to "Log out", logout_path, class: "ui primary button", method: :delete %>
            <% else %>
              <%= link_to "Log in", login_path, class: "ui primary button" %>
          <% end %>
            </div>
        </div>